void swap(char* A, char* B){
	char* temp = A; 
	*A=*B; 
	*B=*temp; 
}
char* reverse(const char* string)
{
	char* reverse; 
	int length = strlen(reverse+1); 
	memcpy(reverse, string); 

	for(int x=0; 
	

}
void main()
{

}